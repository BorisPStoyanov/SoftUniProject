{% extends 'admin.html.twig' %}

{% block body %}
    <h1>Promotion creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create"  class="btn btn-primary"/>
    {{ form_end(form) }}

    <hr>
    <a href="{{ path('admin_promotion_index') }}" class="btn btn-success">Back to the list</a>

{% endblock %}
